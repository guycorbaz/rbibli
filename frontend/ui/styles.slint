

export global Styles {
     // Minimum application window dimensions to ensure usable interface
    out property <length> app_vertical_size: 600px;
    out property <length> app_horizontal_size: 700px;

}